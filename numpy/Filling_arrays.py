import numpy as np

zeros = np.zeros((3,4))
print(zeros)

ones = np.ones((4, 3))
print(ones)

eye = np.eye(4)
print(eye)

linspace = np.linspace(0, 10, 5)
print(linspace)

arange = np.arange(0, 10, 3)
print(arange)